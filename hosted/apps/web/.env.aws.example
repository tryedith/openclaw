# AWS ECS Configuration for OpenClaw Hosted Platform
# Copy this file to .env.local and fill in the values

# Get these values from: terraform output
AWS_REGION=us-west-2
ECS_CLUSTER_NAME=openclaw-cluster
ECS_TASK_DEFINITION=openclaw
ALB_LISTENER_ARN=arn:aws:elasticloadbalancing:us-west-2:ACCOUNT_ID:listener/app/openclaw-alb/xxx/xxx
VPC_ID=vpc-xxx
SUBNET_IDS=subnet-xxx,subnet-xxx
SECURITY_GROUP_ID=sg-xxx

# ECR Repository URL (for reference)
ECR_REPOSITORY_URL=ACCOUNT_ID.dkr.ecr.us-west-2.amazonaws.com/openclaw

# ALB DNS (for instance URLs in HTTP mode)
ALB_DNS_NAME=openclaw-alb-xxx.us-west-2.elb.amazonaws.com

# AWS Credentials (use IAM role in production, credentials for local dev)
# These should be set in your environment or use IAM role
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=

# Supabase service role (required for machine-to-machine usage ingestion API)
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# Shared secret used by OpenClaw instances to call /api/usage/events
USAGE_SERVICE_KEY=your-random-shared-secret

# Provider keys are injected to EC2 gateway containers via AWS Secrets Manager secret:
# openclaw/platform-credentials
# Expected fields:
# - ANTHROPIC_API_KEY
# - OPENAI_API_KEY
# - GEMINI_API_KEY
